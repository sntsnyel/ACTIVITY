<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Switch Activity</title>
    <script language="javascript" type="text/javascript">
    <!--
    var status = true;
    while (status) {
      var grade = prompt("Enter your grade: ");
      var grade_point;
      var grade_input;
      switch (true) {
        case (grade <= 100 && grade >= 96):
          grade_point = 1.00;
          grade_input = "A+";
          document.write("Grade point: " + grade_point+ "</br>");
          document.write("Grade input: " + grade_input+ "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 95 && grade >= 91):
          grade_point = 1.25;
          grade_input = "A";
          document.write("Grade point: " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 90 && grade >= 86):
          grade_point = 1.50;
          grade_input = "A-";
          document.write("Grade point: " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 85 && grade >= 81):
          grade_point = 1.75;
          grade_input = "B+";
          document.write("Grade point: " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 80 && grade >= 75):
          grade_point = 2.00;
          grade_input = "B";
          document.write("Grade point: " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 74 && grade >= 69):
          grade_point = 2.25;
          grade_input = "B-";
          document.write("Grade point: " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 68 && grade >= 63):
          grade_point = 2.50;
          grade_input = "C+";
          document.write("Grade point:  " + grade_point + "</br>");
          document.write("Grade input: " + grade_input + "</br>");
          status = isNAN(grade);
          break;
        case (grade <= 62 && grade >= 57):
         grade_point = 2.75;
         grade_input = "C";
         document.write("Grade point:  " + grade_point + "</br>");
         document.write("Grade input: " + grade_input + "</br>");
         status = isNAN(grade);
         break;
        case (grade <= 56 && grade >= 50):
         grade_point = 3.00;
         grade_input = "C-";
         document.write("Grade point:  " + grade_point + "</br>");
         document.write("Grade input: " + grade_input + "</br>");
         status = isNAN(grade);
         break;
        case (grade <= 49 && grade >= 0):
         grade_point = 5.00;
         grade_input = "F";
         document.write("Grade point:  " + grade_point + "</br>");
         document.write("Grade input: " + grade_input + "</br>");
         status = isNAN(grade);
        break;
        default:
         alert("Re-enter the correct grade");
         status = true;
         }
      }
      -->
    </script>
  </head>
  <body>

  </body>
</html>
